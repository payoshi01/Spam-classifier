# Spam-classifier
Spam Message Classifier is a machine learning web app that detects whether a text message is spam or not. It uses TF-IDF for text preprocessing and a Naive Bayes model trained on the SMS Spam dataset. Built with Python, scikit-learn, and Flask, the app provides a simple browser interface for real-time spam prediction.
